## 1. 統合フォームのサーバ側実装

- [x] 1.1 `app/routes/work-logs.$date.tsx` の action に `saveDay` を追加し、`formData.getAll()` で `allocationId` / `projectId` / `taskId` / `allocatedHours` / `note` を配列として受け取り、添字で行を紐付けて解釈することをテストで確認する
- [x] 1.2 総稼働時間が未保存の日でも `saveDay` が work log を作成し、既存の日は更新することをテストで確認する
- [x] 1.3 案件も時間も未入力の空行が無視され、エラーにもならず allocation も作られないことをテストで確認する
- [x] 1.4 全行を先に検証し、1 行でも不正ならトランザクションを実行せず何も保存されないことをテストで確認する
- [x] 1.5 有効な複数行が 1 回の送信でまとめて作成・更新されることをテストで確認する
- [x] 1.6 前 change で追加した「案件・タスクを維持する」検証が統合フォームでも働くことをテストで確認する（案件変更なしでアーカイブ済み・アサイン解除済みを許可、別案件への変更は拒否、空のタスク送信で元タスク維持）
- [x] 1.7 送信された既存行の全てについて、対象メンバーの配賦かつ対象日付に属することを検証し、1 行でも他者の配賦や別日の配賦が混じっていれば送信全体を 404 で拒否することをテストで確認する
- [x] 1.8 空の新規行に HTML の `required` を付けず、案件・時間の両方が空の行がサーバ側で無視されることを確認する（HTML バリデーションで送信が止まらないことをテストで確認する）

## 2. 行の削除

- [x] 2.1 同一フォーム内の `deleteAllocationId` ボタンで対象行のみが削除され、他の行と総稼働時間が変わらないことをテストで確認する
- [x] 2.2 他人の配賦・存在しない ID を指定した削除が 404 になることをテストで確認する

## 3. 前日から複製

- [x] 3.1 `copyPrevious` で前日の総稼働時間と全 allocation 行（案件・タスク・時間・備考）が複製されることをテストで確認する
- [x] 3.2 対象日に allocation が既に存在する場合、変更を加えずにエラーメッセージを返すことをテストで確認する
- [x] 3.3 前日に work log も allocation も無い場合、変更を加えずに「複製する内容がありません」を返すことをテストで確認する
- [x] 3.4 複製された allocation に複製時点のメンバー原価スナップショットが記録されることをテストで確認する
- [x] 3.5 複製元にアーカイブ済み・アサイン解除済みの案件が含まれる場合、その行をスキップして残りを複製し、スキップした案件名を伝えることをテストで確認する
- [x] 3.6 複製元の全行がスキップ対象の場合、何も変更せず「複製する内容がありません」を返すことをテストで確認する

## 4. 保存フィードバック

- [x] 4.1 保存成功時に保存した allocation 件数を含むメッセージが返り、リダイレクトしないことをテストで確認する
- [x] 4.2 バリデーション失敗時にエラーメッセージが返り、保存を示唆する表現が出ないことをテストで確認する

## 5. 案件の並び順

- [x] 5.1 `effort-allocations` リポジトリに、メンバーの直近 60 日の実績から案件を使用日の降順で返す関数を追加し、順序が仕様どおりになることをテストで確認する
- [x] 5.2 日別入力の loader が、最近使用した案件を先頭に、未使用の案件をその後ろに並べて返すことをテストで確認する
- [x] 5.3 参照済みだがアサイン外の案件（`referencedOnlyProjectIds`）が最近使用順の後ろに配置されることをテストで確認する

## 6. UI の統合

- [x] 6.1 日別入力画面を 1 つの `<Form>` に統合し、総稼働時間・既存行・空の新規行 3 行・保存ボタン・前日複製ボタン・行削除ボタンが同一フォームに並ぶことを確認する
- [x] 6.2 旧 intent（`saveWorkLog` / `addAllocation` / `updateAllocation`）のフォームとサーバ側分岐を削除し、アプリ内に参照が残っていないことを確認する
- [x] 6.3 「削除」「前日から複製」ボタンに `formNoValidate` を付け、空の新規行があってもそのボタンで送信できることを確認する
- [x] 6.4 ロックされた月（`in_review` / `approved`）で全入力と全ボタンが無効になり、送信も 423 で拒否されることをテストで確認する
- [x] 6.5 案件が 0 件のメンバーに対して、空のセレクトだけでなく案内が表示されることを確認する

## 7. テストとドキュメント

- [x] 7.1 `tests/routes/work-logs.test.ts` の旧 intent を使うテスト（`saveWorkLog` 13 件、`addAllocation` 11 件、`updateAllocation` 5 件）を `saveDay` 形式へ移行し、権限・ロック・バリデーション・案件維持の検証意図が維持されていることを確認する
- [x] 7.2 `tests/routes/reports.test.ts` と `tests/routes/observability.test.ts` の旧 intent によるテストデータ作成を `saveDay` 形式へ移行する
- [x] 7.3 `docs/user-guide.md` の「日別工数実績入力」の手順を、1 フォームでまとめて保存する流れと前日複製の説明に更新する
- [x] 7.4 `npm test` / `npm run typecheck` / `npm run lint` がすべて成功することを確認する

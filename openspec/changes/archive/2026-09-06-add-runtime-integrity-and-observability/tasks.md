## 1. DB 接続の整合性設定

- [x] 1.1 `app/db/client.ts` の `createDatabaseConnection()` 内で `foreign_keys = ON` / `journal_mode = WAL` / `busy_timeout = 5000` を設定し、生成した接続で 3 つの pragma が有効になっていることをテストで確認する
- [x] 1.2 `busy_timeout` の既定値が環境変数 `KOSU_SQLITE_BUSY_TIMEOUT_MS` で上書きできるようにし、未設定時は 5000 が使われることをテストで確認する
- [x] 1.3 外部キー制約が実行時に強制されることを確認するテストを追加する（存在しない `project_id` を持つ配賦の挿入が失敗し、`onDelete: "restrict"` が効くことを確認）

## 2. 起動時の孤立データ検査

- [x] 2.1 `app/db/` に `PRAGMA foreign_key_check` を実行して違反をテーブル単位の件数として返す検査関数を追加し、違反が無い場合に空配列を返すことをテストで確認する
- [x] 2.2 検査結果の件数が 0 件のときは起動を継続し、1 件以上のときは対象テーブル名と件数、修復手順への案内を含むエラーを出して非ゼロ終了することをテストで確認する
- [x] 2.3 マイグレーション実行経路（`runMigrationsAndClose` 相当）に検査を組み込み、`npm run db:migrate` でも違反が検出されることをテストで確認する

## 3. CSV 必須列の欠落検出

- [x] 3.1 `app/services/import.ts` に import 種別ごとの必須ヘッダ一覧を定義し、全テンプレート種別で必須列が網羅されていることをテストで確認する
- [x] 3.2 ヘッダ行に必須列が存在しない場合に、列名を含むエラーを返してプレビューもコミットも行わないことをテストで確認する
- [x] 3.3 必須列は存在するが値が空欄の場合に、列欠落とは異なるメッセージで行レベルのバリデーションエラーになることをテストで確認する
- [x] 3.4 必須列を 1 つ欠いた CSV をアップロードしたとき、`NaN` によるトランザクション全体の失敗ではなく列不足のエラーが画面に表示されることをテストで確認する

## 4. 既存実績配賦の案件付け替え防止

- [x] 4.1 `app/routes/work-logs.$date.tsx` の案件セレクトが、編集中の配賦が参照する案件を選択肢に必ず含めるようにし、アサイン解除済み案件・アーカイブ済み案件がラベル付きで表示されることをテストで確認する
- [x] 4.2 既存配賦の更新で `projectId` が元の値と同一の場合、アサイン解除済みまたはアーカイブ済みでも更新が成功して元の案件が維持されることをテストで確認する
- [x] 4.3 既存配賦の更新で `projectId` が別の値に変わる場合、アサインされていない案件またはアーカイブ済み案件への変更が拒否されることをテストで確認する
- [x] 4.4 アーカイブ済みタスクを参照する既存配賦を更新したとき、明示的に別のタスクを選ばない限り元のタスクが維持されることをテストで確認する

## 5. ヘルスチェック端点

- [x] 5.1 `app/routes/health.ts` を追加し、未認証のリクエストに対して DB 接続確認の結果で 200 を返すことをテストで確認する
- [x] 5.2 DB 接続が利用できない状態で 503 を返すことをテストで確認する
- [x] 5.3 ヘルスレスポンスにメンバー・案件・財務情報が含まれないことをテストで確認する
- [x] 5.4 `docker-compose.yml` に `/health` を使う healthcheck を追加し、`docker compose up --build -d` でコンテナが healthy になることを確認する

## 6. 運用イベントのログ記録

- [x] 6.1 `app/lib/log.ts` に `level` / `event` / `message` / `at` と任意コンテキストを 1 行 JSON で標準出力へ書くロガーを追加し、出力形式をテストで確認する
- [x] 6.2 `app/services/auth.ts` の認証失敗と `app/services/login-rate-limit.ts` の制限超過を記録し、パスワード・ハッシュ・セッション ID が出力に含まれないことをテストで確認する
- [x] 6.3 権限拒否（`requireAdministrator`）と保護月への書き込み拒否（`requireOpenMonth`）を記録し、対象月と経路の情報が含まれることをテストで確認する
- [x] 6.4 各ルートの action で未処理例外を捕捉した際にエラーメッセージを記録し、ユーザーへの応答は従来どおり汎用メッセージのままであることをテストで確認する
- [x] 6.5 月次締めの操作（レビュー開始・承認・再オープン・原価補正）を記録し、対象月と実行者の情報が含まれることをテストで確認する
- [x] 6.6 ログ出力の全経路を通して原価率 `hourlyCostRate` が出力に含まれないことをテストで確認する

## 7. ドキュメント

- [x] 7.1 README のバックアップ対象の説明に WAL サイドファイル（`-wal` / `-shm`）を含め、稼働中コピーが unsafe である旨を明記する
- [x] 7.2 README に移行前の `PRAGMA foreign_key_check` による事前確認手順と、起動停止時の修復手順を追記する
- [x] 7.3 README に `/health` 端点とログ出力の説明（収集方法と記録対象）を追記する

## 8. 検証

- [x] 8.1 `npm test` / `npm run typecheck` / `npm run lint` がすべて成功することを確認する
